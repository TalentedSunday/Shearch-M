<% products.each_with_index do |product, index| %>
  <tr>
    <td><%= index + 1 %></td>
    <td><%= product.title %></td>
    <td><%= product.description %></td>
    <td><%= product.tags %></td>
    <td><%= product.country %></td>
    <td>$<%= product.price %></td>
  </tr>
<% end %>